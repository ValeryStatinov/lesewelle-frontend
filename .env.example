# which .env file (.env.development | .env.production) will be used is determined by Vite's built-in convention based on passed -m mode (development | production) in package.json
VITE_BACKEND_API_URL=http://localhost:8000
VITE_AMPLITUDE_API_KEY=thisisampapikey
